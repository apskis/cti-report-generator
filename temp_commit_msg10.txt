BREAKING: Fix Gate 5 to actually call ThreatAnalystAgent

CRITICAL ARCHITECTURAL FIX:
Gate 5 was generating a parallel report structure that was never used.
The AI analyst's output (which became the actual .docx) was never validated by Gate 6.

BEFORE (BROKEN):
```
Collectors → Gates 1-4 (validate raw data)
          → Gate 5 (generates unused draft with old structure)
          → Gate 6 (validates unused draft)  ❌ Never sees real report!

Separately:
ThreatAnalystAgent → WeeklyReportGenerator → .docx  ❌ Not validated!
```

AFTER (FIXED):
```
Collectors → Gates 1-4 (validate raw data)
          → Gate 5 (calls ThreatAnalystAgent, generates analysis)
          → Gate 6 (validates actual AI analysis)
          → WeeklyReportGenerator (formats validated analysis into .docx)
```

CHANGES:

1. Gate 5 now invokes ThreatAnalystAgent directly
   - Weekly: Calls agent.analyze() for tactical analysis
   - Quarterly: Calls agent.analyze_strategic() for strategic analysis
   - Returns AI's output in payload["report"]

2. Gate 6 now validates the ACTUAL report structure
   - Weekly: Validates cve_analysis, apt_activity, statistics, industry_incidents
   - Quarterly: Validates strategic analysis structure
   - Already has report-type-specific validation (commit 62b3035)

3. Removed old Gate 5 logic:
   - No more _format_threat_findings(), _format_resources(), etc.
   - No more WEEKLY_SECTION_ORDER/QUARTERLY_SECTION_ORDER
   - No more unused draft generation

IMPACT:
- Gate 6 now actually validates the report that gets published
- All Gate 6 checks (API citations, statistics accuracy, industry incidents, etc.) now work
- Gate framework is now a true validation pipeline, not a parallel shadow report

BREAKING CHANGE:
Gate 5 payload structure changed from:
```python
{
  "report": {...threat_findings structure...},
  "draft_text": "...",
  "self_check": "..."
}
```

To:
```python
{
  "report": {...cve_analysis, apt_activity, statistics...},  # AI analyst output
  "draft_text": "",  # Not used
  "analysis_type": "tactical" | "strategic"
}
```

This is what should have been built from the start.
