# CTI Report Generator - Environment Variables
# Copy this file to .env and fill in your values
# Note: Most secrets are stored in Azure Key Vault, not here

# Azure Key Vault URL (REQUIRED)
# This is the only secret-related config stored in environment
KEY_VAULT_URL=https://your-keyvault-name.vault.azure.net/

# Comma-separated list of enabled collectors (OPTIONAL)
# Default: all collectors enabled
# Options: nvd, intel471, crowdstrike, threatq, rapid7
ENABLED_COLLECTORS=nvd,intel471,crowdstrike,threatq,rapid7

# Azure Functions local development (auto-set by func start)
# AzureWebJobsStorage=UseDevelopmentStorage=true
# FUNCTIONS_WORKER_RUNTIME=python
